section.section
  header.section__header
    h3#method-isMap.section__h3 isMap(val)
  div.section__content
    dl.section__dl
      dt.section__dt Since：
      dd.section__dd 0.2.0
    h4.section__h4 Description
    p isMap(val) 方法用来检测测试数据是否为 Map 类型。
    h4.section__h4 Parameters
    h5.section__h4 val
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Any
    p （必须）要检测的数据。
    h4.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Boolean。
    p 'val' 是 Map 类型返回 true，否则返回 false。
    pre.section__pre
      code.section__code.
        import Types from '@yaohaixiao/types.js'
        // 或者单独引用 isMap() 方法
        // import isMap from '@yaohaixiao/types.js/isMap'

        const map = new Map([
            ['name', 'Robert'],
            ['Gender', 'Male']
        ])

        Types.is(map) // -> 'map'

        Types.isMap(map) // -> true
        Types.isMap(['name','robert']) // -> false
