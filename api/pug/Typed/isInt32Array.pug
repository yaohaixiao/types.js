section.section
  header.section__header
    h3#method-isInt32Array.section__h3 isInt32Array(val)
  div.section__content
    dl.section__dl
      dt.section__dt Since：
      dd.section__dd 0.2.0
    h4.section__h4 Description
    p isInt32Array(val) 方法用来检测测试数据是否为 Int32Array 类型。
    h4.section__h4 Parameters
    h5.section__h4 val
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Any
    p （必须）要检测的数据。
    h4.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Boolean。
    p 'val' 是 Int32Array 类型，返回 true，否则返回 false。
    pre.section__pre
      code.section__code.
        import Types from '@yaohaixiao/types.js'
        // 或者单独引用 isInt32Array() 方法
        // import isInt32Array from '@yaohaixiao/types.js/isInt32Array'

        const numbers = [ 2, 4 ]
        let args

        function test(a){
          args = arguments
          return a === 'test'
        }

        test()

        Types.isInt32Array(numbers) // -> false
        Types.isInt32Array(new Array(32)) // -> false
        Types.isInt32Array(args) // -> false
        Types.isInt32Array(new Int32Array()) // -> true

