section.section
  header.section__header
    h3#method-isError.section__h3 isError(val)
  div.section__content
    h4.section__h4 Description
    p isError(val) 方法用来检测测试数据是否为 Error 类型。
    h4.section__h4 Parameters
    h5.section__h4 val
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Any
    p （必须）val 待检测的数据。
    h4.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Boolean。
    p true - 表示检测数据为 Error 类型，false 则表示不是。
    pre.section__pre
      code.section__code.
        import Types from '@yaohaixiao/types.js/types'
        // 或者单独引用 isError() 方法
        // import isError from '@yaohaixiao/types.js/isError'

        const error = function Error(){}

        Types.isError(new error()) // -> false
        Types.isError(new Error()) // -> true

