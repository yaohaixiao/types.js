section.section
  header.section__header
    h3#method-isInt32Array.section__h3 isInt32Array(val)
  div.section__content
    h4.section__h4 Description
    p isInt32Array(val) 方法用来检测测试数据是否为 Int32Array 类型。
    h4.section__h4 Parameters
    h5.section__h4 val
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Any
    p （必须）val 待检测的数据。
    h4.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Boolean。
    p true - 表示检测数据是 Int32Array 类型，false 则表示不是。
    pre.section__pre
      code.section__code.
        import Types from '@yaohaixiao/types.js/esm/types'

        const numbers = [ 2, 4 ]

        function test(){
            Types.isInt32Array( arguments ); // -> false
        }

        test();

        Types.isInt32Array( numbers ); // -> false
        Types.isInt32Array( new Array() ); // -> false

        Types.isInt32Array(new Int32Array()) => true

