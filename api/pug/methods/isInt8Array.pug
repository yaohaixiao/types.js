section.section
  header.section__header
    h3#method-isInt8Array.section__h3 isInt8Array(val)
  div.section__content
    h4.section__h4 Description
    p isInt8Array(val) 方法用来检测测试数据是否为 Int8Array 类型。
    h4.section__h4 Parameters
    h5.section__h4 val
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Any
    p （必须）val 待检测的数据。
    h4.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Boolean。
    p true - 表示检测数据是 Int8Array 类型，false 则表示不是。
    pre.section__pre
      code.section__code.
        import Types from '@yaohaixiao/types.js/esm/types'
        // 或者单独引用 isInt8Array() 方法
        // import isInt8Array from '@yaohaixiao/types.js/esm/isInt8Array'

        Types.isInt8Array([]) // -> false
        Types.isInt8Array(new Array(8)) // -> false
        Types.isInt8Array(new Int8Array([])) // -> true
