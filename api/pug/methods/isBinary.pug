section.section
  header.section__header
    h3#method-isBinary.section__h3 isBinary(val)
  div.section__content
    h4.section__h4 Description
    p isBinary(val) 方法用来检测测试数据是二进制（字符串）数据。
    dl.section__dl
      dt.section__dt Since：
      dd.section__dd 1.11.0
    h4.section__h4 Parameters
    h5.section__h4 val
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Buffer | String
    p （必须）val 待检测的数据。
    h4.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Boolean。
    p true - 表示检测数据是二进制（字符串）数据，false 则表示不是。
    pre.section__pre
      code.section__code.
        import Types from '@yaohaixiao/types.js/types'
        // 或者单独引用 isUint16Array() 方法
        // import isBinary from '@yaohaixiao/types.js/isBinary'

        const buffer = Buffer.alloc(15)

        Types.isBinary('Z29vZA==') // -> false
        Types.isBinary(isArrayBuffer(8) // -> false

        Types.is(buffer) // -> 'uint8array'
        isBinary(buffer) // -> true
        isBinary(buffer.toString()) // -> true
