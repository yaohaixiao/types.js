section.section
  header.section__header
    h3#method-isObject.section__h3 isObject(val)
  div.section__content
    h4.section__h4 Description
    p isObject(val) 方法用来检测测试数据是否为 Object 类型。
    h4.section__h4 Parameters
    h5.section__h4 val
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Any
    p （必须）val 待检测的数据。
    h4.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Boolean。
    p true - 表示检测数据是 Object 类型，false 则表示不是。
    pre.section__pre
      code.section__code.
        import Types from '@yaohaixiao/types.js/esm/types'

        Types.isObject({}) // => true
        Types.isObject(null) // => true
        Types.isObject(Object) // => true
        Types.isObject(new Object()) // => true
        Types.isObject([]) // => true
        Types.isObject(() => {}) // => true

        Types.isObject(null) // => false
