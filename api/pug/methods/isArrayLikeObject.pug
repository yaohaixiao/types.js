section.section
  header.section__header
    h3#method-isArrayLikeObject.section__h3 isArrayLikeObject(val)
  div.section__content
    h4.section__h4 Description
    p isArrayLikeObject(val) 方法用来检测测试数据是否为 ArrayLike 的对象数据。
    h4.section__h4 Parameters
    h5.section__h4 val
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Object | Array
    p （必须）val 待检测的数据。
    h4.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Boolean。
    p true - 表示检测数据是 ArrayLike 的对象数据，false 则表示不是。
    pre.section__pre
      code.section__code.
        import Types from '@yaohaixiao/types.js/esm/types'
        // 或者单独引用 isArrayLikeObject() 方法
        // import isArrayLikeObject from '@yaohaixiao/types.js/esm/isArrayLikeObject'

        const arr = [1, 2, 3]
        const empty = () => {}
        let args

        function test(a){
          args = arguments
          return a === 'test'
        }

        test()

        Types.isArrayLikeObject(arr) // -> true
        Types.isArrayLikeObject(empty) // -> false
        Types.isArrayLikeObject(args) // -> true
