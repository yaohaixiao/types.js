section.section
  header.section__header
    h3#method-isValue.section__h3 isValue(val)
  div.section__content
    h4.section__h4 Description
    p isValue(val) 方法用来检测测试数据是否有效的数据（非null、undefined、NaN或者正负无穷数值）。
    dl.section__dl
      dt.section__dt Category：
      dd.section__dd Lang
    h4.section__h4 Parameters
    h5.section__h4 val
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Any
    p （必须）val 待检测的数据。
    h4.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Boolean。
    p true - 表示检测数据是否有效的数据（非null、undefined或者正负无穷数值），false 则表示不是。
    pre.section__pre
      code.section__code.
        import Types from '@yaohaixiao/types.js/types'
        // 或者单独引用 isValue() 方法
        // import isValue from '@yaohaixiao/types.js/isValue'

        const fn = () => {}
        let projects
        function empty(){}

        // 非有效数据
        Types.isValue(projects) // -> false
        Types.isValue(NaN) // -> false
        Types.isValue(null) // -> false
        Types.isValue(Infinity) // -> false
        Types.isValue(-Infinity) // -> false

        // 有效数据
        Types.isValue(0) // -> true
        Types.isValue('') // -> true
        Types.isValue(false) // -> true
        Types.isValue([]) // -> true
        Types.isValue({}) // -> true
        Types.isValue(/\s+/ig) // -> true
        Types.isValue(new Date()) // -> true
        Types.isValue(empty) // -> true
        Types.isValue(fn) // -> true





