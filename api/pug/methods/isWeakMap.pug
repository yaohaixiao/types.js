section.section
  header.section__header
    h3#method-isWeakMap.section__h3 isWeakMap(val)
  div.section__content
    h4.section__h4 Description
    p isWeakMap(val) 方法用来检测测试数据是否为 WeakMap 类型。
    h4.section__h4 Parameters
    h5.section__h4 val
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Any
    p （必须）val 待检测的数据。
    h4.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Boolean。
    p true - 表示检测数据是 WeakMap 类型，false 则表示不是。
    pre.section__pre
      code.section__code.
        import Types from '@yaohaixiao/types.js/types'
        // 或者单独引用 isWeakMap() 方法
        // import isWeakMap from '@yaohaixiao/types.js/isWeakMap'

        const map = new Map([
            ['name', 'Robert'],
            ['Gender', 'Male']
        ])

        const weakmap = new WeakMap()

        weakmap.set({}, 37)

        Types.is(weakmap) // -> 'weakmap'
        Types.isWeakMap(weakmap) // -> true

        Types.is(map) // -> 'map'
        Types.isWeakMap(map) // -> false
