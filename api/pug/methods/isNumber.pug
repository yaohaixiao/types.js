section.section
  header.section__header
    h3#method-isNumber.section__h3 isNumber(val)
  div.section__content
    h4.section__h4 Description
    p isNumber(val) 方法用来检测测试数据是否为 Number 类型。
    h4.section__h4 Parameters
    h5.section__h4 val
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Any
    p （必须）val 待检测的数据。
    h4.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Boolean。
    p true - 表示检测数据是 Number 类型，false 则表示不是。
    pre.section__pre
      code.section__code.
        import Types from '@yaohaixiao/types.js/esm/types'
        // 或者单独引用 isNumber() 方法
        // import isNumber from '@yaohaixiao/types.js/esm/isNumber'

        // True
        Types.isNumber(0) // -> true
        Types.isNumber(.4) // -> true
        Types.isNumber(3.4E2) // -> true
        Types.isNumber(0xffffff) // -> true
        Types.isNumber(NaN) // -> true
        Types.isNumber(Infinity) // -> true
        Types.isNumber(-Infinity) // -> true

        // False
        Types.isNumber('2') // -> false
        Types.isNumber(2 + '1') // -> false
        Types.isNumber(new Number()) // -> false
