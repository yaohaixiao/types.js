section.section
  header.section__header
    h3#method-isEven.section__h3 isEven(val)
  div.section__content
    h4.section__h4 Description
    p isEven(val) 方法用来检测测试数据的(数值)是否为偶数。
    h4.section__h4 Parameters
    h5.section__h4 val
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Number
    p （必须）val 待检测的数据。
    h4.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Boolean。
    p true - 表示检测数据(数值)为偶数，false 则表示不是。
    pre.section__pre
      code.section__code.
        import Types from '@yaohaixiao/types.js/esm/types'
        // 或者单独引用 isEven() 方法
        // import isEven from '@yaohaixiao/types.js/esm/isEven'

        Types.isEven(2) // -> true
        Types.isEven(2.0) // -> true
        Types.isEven('2') // -> false

        Types.isEven(3) // -> false
        Types.isEven(2.2) // -> false
