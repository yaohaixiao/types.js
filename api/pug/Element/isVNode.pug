section.section
  header.section__header
    h3#method-isVNode.section__h3 isVNode(val)
  div.section__content
    dl.section__dl
      dt.section__dt Since：
      dd.section__dd 0.2.0
    h4.section__h4 Description
    p isVNode(val) 方法用来检测测试数据是否为 VNode (VUE虚拟节点)类型。
    h4.section__h4 Parameters
    h5.section__h4 val
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Any
    p （必须）要检测的数据。
    h4.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Boolean。
    p 'val' 是 VNode (VUE虚拟节点)类型，返回 true，否则返回 false。
    pre.section__pre
      code.section__code.
        &lt;template&gt;
           &lt;dev-header ref="header"&gt;&lt;/dev-header&gt;
        &lt;/template&gt;

        &lt;script&gt;
        import Types from '@yaohaixiao/types.js'
        // 或者单独引用 isVNode() 方法
        // import isVNode from '@yaohaixiao/types.js/isVNode'

        export default {
          name: 'PageHeader',
          mounted() {
            const $header = this.$refs.header

            Types.isVNode($header) // -> true
          }
        }
        &lt;/script&gt;
