section.section
  header.section__header
    h3#method-isBlank.section__h3 isBlank(val)
  div.section__content
    dl.section__dl
      dt.section__dt Since：
      dd.section__dd 0.2.0
    h4.section__h4 Description
    p isBlank(val) 方法用来检测测试数据是否只包含空格。
    h4.section__h4 Parameters
    h5.section__h4 val
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Any
    p （必须）val 待检测的数据。
    h4.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Boolean。
    p true - 表示检测数据是空值，false 则表示不是。
    pre.section__pre
      code.section__code.
        import Types from '@yaohaixiao/types.js'
        // 或者单独引用 isBlank() 方法
        // import isBlank from '@yaohaixiao/types.js/isBlank'

        Types.isBlank('  ') // -> true
        Types.isBlank(new String()) // -> false
        Types.isBlank('') // -> true
