section.section
  header.section__header
    h3#method-isEmpty.section__h3 isEmpty(val)
  div.section__content
    dl.section__dl
      dt.section__dt Since：
      dd.section__dd 0.2.0
    h4.section__h4 Description
    p isEmpty(val) 方法用来检测测试数据是否为空字符串、数组、对象或者空的 arguments 参数。
    h4.section__h4 Parameters
    h5.section__h4 val
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Any
    p （必须）val 待检测的数据。
    h4.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Boolean。
    p true - 表示检测数据为空字符串，false 则表示不是。
    pre.section__pre
      code.section__code.
        import Types from '@yaohaixiao/types.js'
        // 或者单独引用 isEmpty() 方法
        // import isEmpty from '@yaohaixiao/types.js/isEmpty'

        Types.isEmpty(null) // -> true

        // 字符串
        Types.isEmpty('') // -> true
        Types.isEmpty(String()) // -> true
        Types.isEmpty(' ') // -> false

        // 数组
        Types.isEmpty([]) // -> true
        Types.isEmpty([1]) // -> false

        // 对象
        Types.isEmpty(new String()) // -> true
        Types.isEmpty({}) // -> true
        Types.isEmpty({a: 1}) // -> false

        // arguments 参数
        let args
        const fn = function () {arg = arguments}

        fn()

        Types.isEmpty(fn.prototype) // -> false
        Types.isEmpty(args) // -> true

        fn(1)

        Types.isEmpty(args) // -> false

        // buffer
        left buffer = Buffer.alloc(0)

        isEmpty(buffer) => true

        buffer = Buffer.alloc(10)

        isEmpty(buffer) => false
