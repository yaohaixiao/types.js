section.section
  header.section__header
    h3#method-isSafeInteger.section__h3 isSafeInteger(val)
  div.section__content
    dl.section__dl
      dt.section__dt Since：
      dd.section__dd 2.7.0
    h4.section__h4 Description
    p isSafeInteger(val) 方法用来检测测试数据的(数值)是否为整数。
    h4.section__h4 Parameters
    h5.section__h4 val
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Any
    p （必须）要检测的数据。
    h4.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Boolean。
    p 'val' 是安全的 Integer 值，返回 true，否则返回 false。
    pre.section__pre
      code.section__code.
        import Types from '@yaohaixiao/types.js'
        // 或者单独引用 isSafeInteger() 方法
        // import isSafeInteger from '@yaohaixiao/types.js/isSafeInteger'

        isSafeInteger(3) // => true

        isSafeInteger(Number.MIN_VALUE) // => false

        isSafeInteger(Infinity) // => false

        isSafeInteger('3') // => false
