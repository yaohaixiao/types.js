export default isIDNumber;
/**
 * 验证是否为（中国）身份证号码
 * ========================================================================
 * @method isIDNumber
 * @param {String} idNumber
 * @return {Boolean}
 */
declare function isIDNumber(idNumber: string): boolean;
